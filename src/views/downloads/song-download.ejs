<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link rel="stylesheet" href="/css/style.css">
    <link rel="stylesheet" href="/css/additional-styles.css">
    <link rel="stylesheet" href="/css/downloads.css">
</head>
<body>
    <%- include('../partials/header', { user: user }) %>

    <div class="album-download-container">
        <div class="album-header">
            <div>
                <% if (items[0].cover_url) { %>
                    <img src="<%= items[0].cover_url %>" alt="<%= items[0].album %>" class="album-cover">
                <% } else { %>
                    <div class="album-cover" style="background: rgba(24, 69, 81, 0.3); display: flex; align-items: center; justify-content: center; color: #8ab7ce; font-size: 3rem;">
                        🎵
                    </div>
                <% } %>
            </div>
            
            <div class="album-info">
                <h1><%= items[0].name %></h1>
                <div class="artist"><%= items[0].artist %></div>
                <p style="color: #8ab7ce; margin-top: 0.5rem;">from "<%= items[0].album %>"</p>
                
                <div class="album-details">
                    <div class="detail-item">
                        <div class="detail-label">Purchase Date</div>
                        <div class="detail-value"><%= new Date(purchaseDate).toLocaleDateString() %></div>
                    </div>
                    
                    <div class="detail-item">
                        <div class="detail-label">Price</div>
                        <div class="detail-value">$<%= parseFloat(items[0].price).toFixed(2) %></div>
                    </div>
                </div>
            </div>
        </div>

        <div class="download-options">
            <h2>Download Options</h2>
            
            <div class="format-grid">
                <div class="format-option">
                    <div class="format-icon">🎵</div>
                    <div class="format-name">MP3 Download</div>
                    <div class="format-description">High-quality MP3 file, ready for any device</div>
                    <div class="format-size">~5-8MB estimated</div>
                    <a href="/download-file/<%= token %>/<%= items[0].id %>" 
                       class="download-btn" download="<%= items[0].name %>.mp3">Download MP3</a>
                </div>

                <div class="format-option">
                    <div class="format-icon">🎶</div>
                    <div class="format-name">WAV Download</div>
                    <div class="format-description">Lossless audio file for audiophiles</div>
                    <div class="format-size">~25-40MB estimated</div>
                    <a href="/download-file/<%= token %>/<%= items[0].id %>" 
                       class="download-btn" download="<%= items[0].name %>.wav">Download WAV</a>
                </div>
            </div>

            <div class="download-info">
                <h3>Download Information</h3>
                <ul>
                    <li>Your download link expires on <%= new Date(expiresAt).toLocaleDateString() %></li>
                    <li>MP3 files are 320kbps quality</li>
                    <li>WAV files are CD-quality lossless</li>
                    <li>Files include embedded metadata and artwork</li>
                </ul>
            </div>
        </div>

        <div style="text-align: center; margin: 2rem 0;">
            <a href="/" class="download-btn secondary">← Browse More Music</a>
        </div>
    </div>

    <%- include('../partials/footer') %>
</body>
</html>